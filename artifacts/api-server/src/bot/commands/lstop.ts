import { ChatInputCommandInteraction, SlashCommandBuilder } from "discord.js";import { getLavalinkPlayer } from "../lavalink.js";export const data=new SlashCommandBuilder().setName("lstop").setDescription("Para o Lavalink paralelo e sai do canal");export async function execute(i:ChatInputCommandInteraction){const p=getLavalinkPlayer(i.guildId??"");if(!p)return void i.reply({content:"❌ Não há player Lavalink.",ephemeral:true});p.queue.clear();p.destroy();await i.reply("⏹️ Lavalink paralelo parado.");}
