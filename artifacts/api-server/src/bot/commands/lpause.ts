import { ChatInputCommandInteraction, SlashCommandBuilder } from "discord.js";import { getLavalinkPlayer } from "../lavalink.js";export const data=new SlashCommandBuilder().setName("lpause").setDescription("Pausa a música do Lavalink paralelo");export async function execute(i:ChatInputCommandInteraction){const p=getLavalinkPlayer(i.guildId??"");if(!p?.playing)return void i.reply({content:"❌ Não há música no Lavalink paralelo.",ephemeral:true});await p.pause(true);await i.reply("⏸️ Música Lavalink pausada.");}
