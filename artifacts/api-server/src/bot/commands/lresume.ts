import { ChatInputCommandInteraction, SlashCommandBuilder } from "discord.js";import { getLavalinkPlayer } from "../lavalink.js";export const data=new SlashCommandBuilder().setName("lresume").setDescription("Retoma a música do Lavalink paralelo");export async function execute(i:ChatInputCommandInteraction){const p=getLavalinkPlayer(i.guildId??"");if(!p)return void i.reply({content:"❌ Não há player Lavalink.",ephemeral:true});await p.pause(false);await i.reply("▶️ Música Lavalink retomada.");}
