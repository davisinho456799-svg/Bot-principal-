import { ChatInputCommandInteraction, SlashCommandBuilder } from "discord.js";import { getLavalinkPlayer } from "../lavalink.js";export const data=new SlashCommandBuilder().setName("lskip").setDescription("Pula a música do Lavalink paralelo");export async function execute(i:ChatInputCommandInteraction){const p=getLavalinkPlayer(i.guildId??"");if(!p)return void i.reply({content:"❌ Não há player Lavalink.",ephemeral:true});const title=p.current?.info?.title??"faixa atual";await p.stop();await i.reply("⏭️ Pulei **"+title+"** no Lavalink paralelo.");}
