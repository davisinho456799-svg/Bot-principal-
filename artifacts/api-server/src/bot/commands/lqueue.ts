import { ChatInputCommandInteraction, SlashCommandBuilder } from "discord.js";import { getLavalinkPlayer } from "../lavalink.js";export const data=new SlashCommandBuilder().setName("lqueue").setDescription("Mostra a fila do Lavalink paralelo");export async function execute(i:ChatInputCommandInteraction){const p=getLavalinkPlayer(i.guildId??"");if(!p)return void i.reply({content:"❌ Não há fila Lavalink.",ephemeral:true});const current=p.current?.info?.title??"nada",next=p.queue?.map((t:any,n:number)=>(n+1)+". "+(t.info?.title??"sem título")).join("\n")||"fila vazia";await i.reply("🎵 **Atual:** "+current+"\n**Próximas:**\n"+next);}
